import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({

  mainContainer: {
  
    padding: 5,
    flexDirection: "row",
    padding: 5,
    alignItems: "center"
  },
});
export default styles;